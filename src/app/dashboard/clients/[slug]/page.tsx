import DisplayClientPage from "@/components/Pages/DisplayClient";

export const metadata = {
  title: "Gro - Assign Meal Plan",
  icons: {
    icon: "/gro_logo.png",
  },
};

const DisplayClient = () => {
  return <DisplayClientPage />;
};

export default DisplayClient;
