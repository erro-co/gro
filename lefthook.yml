commit-msg:
  commands:
    lint-commit-msg:
      run: npx commitlint --edit
pre-commit:
  parallel: true
  commands:
    # test:
    #   run: npm run test
    lint:
      run: npm run lint:all
    format:
      run: npm run format:all
