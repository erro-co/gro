commit-msg:
  commands:
    lint-commit-msg:
      run: npx commitlint --edit
pre-commit:
  parallel: true
  commands:
    # test:
    #   run: npm run test
    build:
      run: npm run build
    lint:
      run: npm run lint:all
    format:
      run: npm run format:all
